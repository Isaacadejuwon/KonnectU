# Admin Frontend (Next.js)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Notes:
- Dev server runs on port 3001 by default.
- Set `NEXT_PUBLIC_API_URL` to your backend API base URL.
- Login page and a simple 2FA flow stub are provided; integrate with backend OTP endpoints.
